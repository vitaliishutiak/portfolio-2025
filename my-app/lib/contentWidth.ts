/** Максимальна ширина основного контенту на десктопі */
export const CONTENT_MAX_WIDTH_PX = 1520;

/** Висота фіксованого хедера (Toolbar у Header.tsx) */
export const HEADER_TOOLBAR_HEIGHT_PX = 64;

/** Половина контенту мінус типовий gap 20px (для двоколонкових блоків) */
export const CONTENT_HALF_COLUMN_PX = Math.floor((CONTENT_MAX_WIDTH_PX - 20) / 2);
